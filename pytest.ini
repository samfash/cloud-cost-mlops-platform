[pytest]
testpaths = tests/unit tests/integration
python_files = test_*.py
addopts = -q --tb=short -m "not slow"
markers =
    slow: end-to-end pipeline runs
filterwarnings =
    ignore::DeprecationWarning
