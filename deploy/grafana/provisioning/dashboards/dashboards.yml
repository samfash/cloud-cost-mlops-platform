apiVersion: 1
providers:
  - name: cloud-cost
    orgId: 1
    folder: Cloud Cost
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
